import React from 'react'
import './cstyleui.css'

const Button37 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button37">
        {label}
    </button>
  )
}

export default Button37
