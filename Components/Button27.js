import React from 'react'
import './cstyleui.css'

const Button27 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button27">
        {label}
    </button>
  )
}

export default Button27
