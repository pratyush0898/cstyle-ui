import React from 'react'
import './cstyleui.css'

const Button39 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button39">
        {label}
    </button>
  )
}

export default Button39
