import React from 'react'
import './cstyleui.css'

const Button33 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button33">
        {label}
    </button>
  )
}

export default Button33
