import React from 'react'
import './cstyleui.css'

const Button43 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button43">
        {label}
    </button>
  )
}

export default Button43
