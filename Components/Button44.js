import React from 'react'
import './cstyleui.css'

const Button44 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button44">
        {label}
    </button>
  )
}

export default Button44
