import React from 'react'
import './cstyleui.css'

const Button35 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button35">
        {label}
    </button>
  )
}

export default Button35
