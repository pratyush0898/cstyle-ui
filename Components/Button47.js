import React from 'react'
import './cstyleui.css'

const Button47 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button47">
        {label}
    </button>
  )
}

export default Button47
