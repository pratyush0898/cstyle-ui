import React from 'react'
import './cstyleui.css'

const Button36 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button36">
        {label}
    </button>
  )
}

export default Button36
