import React from 'react'
import './cstyleui.css'

const Button49 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button49">
        {label}
    </button>
  )
}

export default Button49
