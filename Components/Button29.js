import React from 'react'
import './cstyleui.css'

const Button29 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button29">
        {label}
    </button>
  )
}

export default Button29
