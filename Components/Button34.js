import React from 'react'
import './cstyleui.css'

const Button34 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button34">
        {label}
    </button>
  )
}

export default Button34
