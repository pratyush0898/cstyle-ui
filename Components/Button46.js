import React from 'react'
import './cstyleui.css'

const Button46 = ({ label, onClick, style }) => {
  return (
    <button onClick={onClick} style={style} className="cstyle-button46">
        {label}
    </button>
  )
}

export default Button46
